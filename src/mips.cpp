/**
Registers mapped to hashmap 
instruction set keeps track of each instructions values from files
create parsefile class to iterate over file and create instructions
mips class will read files and contain all instructions, symbol table, and memory


 */